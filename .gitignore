# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
/build

# misc
.DS_Store
*.pem

# debug logs (package-manager)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env*
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ── Pre-commit cleanup (deployment prep) — root-anchored, audited ──
# root logs & backups
/*.log
/*.backup

# root one-off pipeline/repair scripts (production code lives in src|app|components|lib|scripts)
/*.py
/*.ps1
/*.bat
/*.mjs
/*.js

# generated reports/dumps (root only)
/*.txt
/*.csv
/*.html
/*.md
!README.md

# root JSON dumps — blanket with explicit production whitelist
/*.json
!package.json
!package-lock.json
!tsconfig.json
!phase1-products.json
!improved-matches.json
!outlet_brands.json
!beautybox-products.json
!beautybox-api-products.json

# named junk / temp artifacts (verified in audit)
/$null
/{
/phase1-implementation.ts
/db/_audit_tmp.mjs

# local logs and temporary artifacts
*.log
*.bak
*.backup
*-backup.*
*.tmp
tmp/
verified/
cutout-evidence/
.backup-outlet-batch1/
public/uploads/

# local analysis / generated working files
scripts/_medium20-dump.txt
